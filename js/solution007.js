//Write a function which converts the input string to uppercase.
function makeUpperCase(str) {
    let sentence = str
    return sentence.toUpperCase(str)
}

//function makeUpperCase(str) {return str.toUpperCase();}